# OpenDota API key (optional but recommended for higher rate limits)
OPENDOTA_KEY=

# Optional: where to sync `data/processed/` + `data/metrics/` via rsync over SSH
# Example: user@your-vps:/srv/dota-datas
DOTA_DATA_REMOTE=
